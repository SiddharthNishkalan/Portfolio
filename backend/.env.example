# .env.example — Copy this to .env and fill in your values
# Never commit .env to git!

# ── Flask ──
SECRET_KEY=9vR@8L!xP2#nQ7zF$wK4mA1bH5sY
FLASK_DEBUG=true
PORT=5000

# ── Database ──
# Leave blank to use SQLite (default, auto-created at backend/database/portfolio.db)
# DATABASE_URL=postgresql://user:password@localhost/portfolio

# ── Email (Gmail SMTP) ──
# 1. Enable 2FA on your Gmail account
# 2. Go to Google Account → Security → App Passwords
# 3. Generate an app password and paste it below
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=s.siddharthnishkalan@gmail.com
MAIL_PASSWORD=juiz lrtl sedp acmj
MAIL_RECEIVER=s.siddharthnishkalan@gmail.com

# ── GitHub ──
GITHUB_USERNAME=siddharthnishkalan
# Optional: create a token at github.com/settings/tokens (no scopes needed for public repos)
# Avoids the 60 req/hour unauthenticated limit
GITHUB_TOKEN=

# ── CORS — comma-separated origins allowed to call the API ──
ALLOWED_ORIGINS=http://localhost:5500,http://127.0.0.1:5500,https://yourdomain.com
